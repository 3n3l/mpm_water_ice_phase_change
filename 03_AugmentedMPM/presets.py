from _common.presets import water_presets, mixed_presets, snow_presets, ice_presets

# configuration_list = water_presets + mixed_presets + snow_presets + ice_presets
# TODO: augmented solver can't handle snow atm
configuration_list = water_presets + mixed_presets + ice_presets
